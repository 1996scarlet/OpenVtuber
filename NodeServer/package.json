{
  "name": "y",
  "version": "1.0.0",
  "description": "sudo apt install nodejs npm npm init npm install --save express npm install --save socket.io",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "socket.io": "^2.2.0"
  }
}
